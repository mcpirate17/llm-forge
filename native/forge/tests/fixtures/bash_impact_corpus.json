[
  "echo hi",
  "ls -la {ROOT}",
  "git status",
  "find {ROOT} -type f",
  "cat {ROOT}/small/a.txt",
  "sqlite3 {ROOT}/test.db 'SELECT * FROM items'",
  "rm -rf {ROOT}/missing_target",
  "find {ROOT}/missing_target -delete",
  "git clean -fdx {ROOT}/missing_target",
  "rm -rf {ROOT}/small",
  "rm -rf {ROOT}/nested",
  "rm -rf {ROOT}/dotdir",
  "rm -rf {ROOT}/large_tree",
  "rm -rf {ROOT}/outer_dir",
  "rm -rf {ROOT}/symlink_to_file",
  "rm -rf {ROOT}/small {ROOT}/nested",
  "find {ROOT}/large_tree -delete",
  "git clean -fdx {ROOT}/nested",
  "sqlite3 {ROOT}/test.db \"DELETE FROM items WHERE status='done'\"",
  "sqlite3 {ROOT}/test.db \"DELETE FROM items\"",
  "sqlite3 {ROOT}/test.db \"DROP TABLE items\"",
  "sqlite3 {ROOT}/missing.db \"DELETE FROM items\"",
  "rm -rf \"{ROOT}/small\"",
  "rm -rf {ROOT}/small && echo done",
  "rm -fr {ROOT}/nested",
  "rm -Rf {ROOT}/dotdir",
  "sqlite3 {ROOT}/test.db \"UPDATE items SET status='x'\"",
  "sqlite3 {ROOT}/test.db \"TRUNCATE items\"",
  "find \"{ROOT}/large_tree\" -type f -delete",
  "rm -rf {ROOT}/small {ROOT}/missing_target",
  "rm -rf {ROOT}/large_tree; find {ROOT}/nested -delete",
  "echo 'rm -rf nothing happens here'"
]
